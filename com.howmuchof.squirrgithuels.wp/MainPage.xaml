<phone:PhoneApplicationPage
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
                            xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:ignore="http://www.ignore.com"
                            xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
                            xmlns:converters="clr-namespace:com.howmuchof.squirrgithuels.wp.Converters" x:Class="com.howmuchof.squirrgithuels.wp.MainPage"
                            mc:Ignorable="d ignore"
                            SupportedOrientations="PortraitOrLandscape"
                            Orientation="Portrait"
                            shell:SystemTray.IsVisible="True"
                            >


    <phone:PhoneApplicationPage.Resources>
        <converters:TimeConverter x:Key="TimeConverter"/>
        <converters:DateConverter x:Key="DateConverter"/>
    </phone:PhoneApplicationPage.Resources>

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Assets/AppBar/add.png" 
		    Text="Добавить" Click="AddButtonClick" />
            <!--<shell:ApplicationBarIconButton IconUri="/Assets/AppBar/add.png" 
		    Text="Добавить"/>-->
            <!--<shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>-->
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot"
		Background="Transparent"
		VerticalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->

        <phone:Pivot Title="{Binding LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}" 
			Grid.RowSpan="2">

            <phone:PivotItem Header="{Binding LocalizedResources.MainPage, Source={StaticResource LocalizedStrings}}">

                <StackPanel>
                    <TextBlock Text="{Binding LocalizedResources.AppDiscription, Source={StaticResource LocalizedStrings}}"
						Style="{StaticResource PhoneTextNormalStyle}"
						TextWrapping="Wrap"
						FontSize="25"/>

                    <Button Content="{Binding LocalizedResources.NewParametrs, Source={StaticResource LocalizedStrings}}" 
						Margin="0,50,0,0"/>

                    <Button Content="{Binding LocalizedResources.AppBarButtonText, Source={StaticResource LocalizedStrings}}"/>
                    <Button Content="{Binding LocalizedResources.AppGraphText, Source={StaticResource LocalizedStrings}}"/>

                </StackPanel>

            </phone:PivotItem>

            <phone:PivotItem Header="{Binding LocalizedResources.ListPage, Source={StaticResource LocalizedStrings}}">
                <!--<Grid >
                    <Grid.RowDefinitions>
                	    <RowDefinition Height="auto"/>
                	    <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Время" FontSize="30"      HorizontalAlignment="Center"  Grid.Row="0"/>
                    <TextBlock Text="Количество" FontSize="30" HorizontalAlignment="Center" Grid.Row="0" Grid.Column="1"/>
                    
                    <ListBox ItemsSource="{Binding DataService}" 
                                Grid.Row="1" 
                                Grid.ColumnSpan="2" Grid.Column="0">
                        
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,15,0,0">
                                    <TextBlock Text="{Binding Date}"  FontSize="40"/>
                                    <TextBlock Text="{Binding Count}" FontSize="40" HorizontalAlignment="Right" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                
                </Grid>-->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Время"      FontSize="30" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Количество" FontSize="30" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" />


                    <phone:LongListSelector ItemsSource="{Binding DataService}" Grid.ColumnSpan="2" Grid.Column="0"
						Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <phone:LongListSelector.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,15,0,0" toolkit:TiltEffect.IsTiltEnabled="True">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Text="{Binding Date, Converter={StaticResource TimeConverter}}"  FontSize="40"/>
                                    <TextBlock Text="{Binding Count}" FontSize="40" Grid.Row="0" Grid.Column="1" HorizontalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </phone:LongListSelector.ItemTemplate>
                    </phone:LongListSelector>
                </Grid>
            </phone:PivotItem>

        </phone:Pivot>


        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>